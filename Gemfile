source "https://rubygems.org"

# Mirrors the GitHub Pages build environment (pulls in the right Jekyll
# version plus jekyll-remote-theme, sitemap, seo-tag, feed, paginate, etc.)
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.x (WEBrick is no longer bundled).
gem "webrick"
